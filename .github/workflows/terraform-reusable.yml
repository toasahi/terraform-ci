name: Terraform Workflow (Reusable)

on:
  workflow_call:
    inputs:
      environment:
        description: 'Environment name'
        required: true
        type: string
      pr_number:
        description: 'Pull request number (for artifact naming)'
        required: false
        type: string
        default: ''
      trigger_ref:
        description: 'Git reference that triggered the workflow'
        required: false
        type: string
        default: ''
      is_apply:
        description: 'Whether to run apply after plan (true for PR merge, false for PR open/sync)'
        required: false
        type: boolean
        default: false

jobs:
  plan:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    # Add permissions for OIDC
    permissions:
      id-token: write
      contents: read
      pull-requests: write
      issues: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.trigger_ref }}
    
      - name: debug
        run: |
            echo "${{ vars.AWS_ROLE_ARN}}"
            echo "${{ inputs.environment}}"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ap-northeast-1
      
      - name: tfcmt install
        run: |
          # Download tfcmt for Linux x64
          gh release download -R suzuki-shunsuke/tfcmt v4.14.0 -p tfcmt_linux_amd64.tar.gz
          
          # Verify the downloaded file
          gh attestation verify tfcmt_linux_amd64.tar.gz -R suzuki-shunsuke/tfcmt --signer-workflow suzuki-shunsuke/go-release-workflow/.github/workflows/release.yaml
          
          # Extract and install
          tar -xzf tfcmt_linux_amd64.tar.gz
          chmod +x tfcmt
          sudo mv tfcmt /usr/local/bin/
          
          # Verify installation
          tfcmt --version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.12.2

      - name: Terraform Init
        run: |
            cd environments/${{ inputs.environment }}
            terraform init -reconfigure

      - name: Terraform Plan
        run: |
          cd environments/${{ inputs.environment }}
          terraform plan -out=tfplan
          tfcmt plan --patch -- terraform show -no-color tfplan
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Upload Terraform Plan
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan-${{ inputs.pr_number }}-${{ inputs.environment }}
          path: |
            environments/${{ inputs.environment }}/tfplan
            environments/${{ inputs.environment }}/.terraform.lock.hcl
          retention-days: 5
          # Overwrite existing artifact if it exists
          overwrite: true

      - name: Debug - List files to verify .terraform.lock.hcl exists
        run: |
          echo "Files in environments/${{ inputs.environment }}:"
          ls -la environments/${{ inputs.environment }}/
          echo "Checking for .terraform.lock.hcl:"
          if [ -f "environments/${{ inputs.environment }}/.terraform.lock.hcl" ]; then
            echo "✓ .terraform.lock.hcl found"
            ls -la environments/${{ inputs.environment }}/.terraform.lock.hcl
          else
            echo "✗ .terraform.lock.hcl not found"
          fi
          echo "Checking for tfplan:"
          if [ -f "environments/${{ inputs.environment }}/tfplan" ]; then
            echo "✓ tfplan found"
            ls -la environments/${{ inputs.environment }}/tfplan
          else
            echo "✗ tfplan not found"
          fi

  apply:
    needs: plan
    if: inputs.is_apply == true
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}_apply

    # Add permissions for OIDC
    permissions:
      id-token: write
      contents: read
      pull-requests: write
      issues: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.trigger_ref }}

      - name: Download Terraform Plan
        uses: actions/download-artifact@v4
        with:
          name: terraform-plan-${{ inputs.pr_number }}-${{ inputs.environment }}
          path: downloaded-artifacts/

      - name: Debug - Check downloaded artifacts
        run: |
          echo "Downloaded artifacts structure:"
          find downloaded-artifacts/ -type f -exec ls -la {} \;

      - name: Move artifacts to correct location
        run: |
          mkdir -p environments/${{ inputs.environment }}/
          mv downloaded-artifacts/environments/${{ inputs.environment }}/tfplan environments/${{ inputs.environment }}/tfplan
          if [ -f "downloaded-artifacts/environments/${{ inputs.environment }}/.terraform.lock.hcl" ]; then
            mv downloaded-artifacts/environments/${{ inputs.environment }}/.terraform.lock.hcl environments/${{ inputs.environment }}/.terraform.lock.hcl
            echo "✓ .terraform.lock.hcl moved successfully"
          else
            echo "⚠️ .terraform.lock.hcl not found in artifacts, will use existing one from checkout"
          fi

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ap-northeast-1
      
      - name: tfcmt install
        run: |
          # Download tfcmt for Linux x64
          gh release download -R suzuki-shunsuke/tfcmt v4.14.0 -p tfcmt_linux_amd64.tar.gz
          
          # Verify the downloaded file
          gh attestation verify tfcmt_linux_amd64.tar.gz -R suzuki-shunsuke/tfcmt --signer-workflow suzuki-shunsuke/go-release-workflow/.github/workflows/release.yaml
          
          # Extract and install
          tar -xzf tfcmt_linux_amd64.tar.gz
          chmod +x tfcmt
          sudo mv tfcmt /usr/local/bin/
          
          # Verify installation
          tfcmt --version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.12.2

      - name: Terraform Init
        run: |
            cd environments/${{ inputs.environment }}
            terraform init -reconfigure

      - name: Verify Plan File
        run: |
          if [ ! -f "environments/${{ inputs.environment }}/tfplan" ]; then
            echo "Error: Plan file not found!"
            echo "Files in environments/${{ inputs.environment }}:"
            ls -la environments/${{ inputs.environment }}/ || echo "Directory does not exist"
            exit 1
          fi
          echo "Plan file found, proceeding with apply..."

      - name: Terraform Apply
        run: |
            cd environments/${{ inputs.environment }}
            tfcmt apply -- terraform apply tfplan
        env:
          GITHUB_TOKEN: ${{ github.token }}